import type { ReactElement } from 'react';

import SignInLoader from '@/ui/Loaders/SignInLoader';

const SignInloading = (): ReactElement => {
  return <SignInLoader />;
};

export default SignInloading;
