import type { ReactElement } from 'react';

import SignUpLoader from '@/ui/Loaders/SignUpLoader';

const SignUploading = (): ReactElement => {
  return <SignUpLoader />;
};

export default SignUploading;
