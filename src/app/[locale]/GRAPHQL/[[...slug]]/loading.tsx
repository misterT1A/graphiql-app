import type { ReactElement } from 'react';

import GraphClientLoader from '@/ui/Loaders/GraphClientLoader';

const Graphloading = (): ReactElement => {
  return <GraphClientLoader />;
};

export default Graphloading;
