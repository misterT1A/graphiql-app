import type { ReactElement } from 'react';

import RestClientLoader from '@/ui/Loaders/RestClientLoader';

const Restloading = (): ReactElement => {
  return <RestClientLoader />;
};

export default Restloading;
