import type { ReactElement } from 'react';

import HistoryLoader from '@/ui/Loaders/HistoryLoader';

const Historyloading = (): ReactElement => {
  return <HistoryLoader />;
};

export default Historyloading;
